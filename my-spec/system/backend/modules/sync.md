# 后端模块：已停用同步链路

## 当前结论

- 当前 `api_v1_router` 没有注册 `/sync` 路由
- 当前移动端主链路也不再使用多设备同步入口
- `user_device_sync_states` 等历史概念只存在于旧迁移/旧文档语境中

## 处理原则

- 不再把同步能力写成“当前功能”
- 如果未来恢复同步，需要重新补齐：
  - 路由注册
  - service
  - 前端入口
  - schema 字典
