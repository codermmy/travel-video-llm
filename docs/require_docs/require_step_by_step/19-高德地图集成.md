# 任务 ID: 18 - expo-router 导航配置

## 📋 基本信息

| 项目 | 内容 |
|------|------|
| **任务名称** | expo-router 导航配置 |
| **所属阶段** | Stage-06 导航与地图 |
| **预估工期** | 0.5 天 |
| **前置条件** | Task-17 故事生成 |

---

## 1. 任务目标

使用 expo-router 配置完整的导航结构，包括认证流程和主页。

## 2. 实现细节

### 2.1 文件结构

```
app/
├── _layout.tsx           # 根布局（认证检查）
├── (auth)/               # 未认证用户
│   ├── _layout.tsx
│   └── index.tsx         # 欢迎页
├── (tabs)/               # 已认证用户
│   ├── _layout.tsx       # Tab 导航
│   ├── index.tsx         # 主页（地图）
│   └── events.tsx        # 事件列表（可选）
└── _sitemap.tsx          # 站点地图（可选）
```

### 2.2 根布局 `app/_layout.tsx`

```typescript
/**
 * 根布局 - 认证检查与重定向
 */
import { useEffect } from 'react';
import { Stack } from 'expo-router';
import { useAuthStore } from '@/stores';
import { ActivityIndicator } from 'react-native';

export default function RootLayout() {
  const { isAuthenticated, isLoading, checkAuth } = useAuthStore();

  useEffect(() => {
    checkAuth();
  }, []);

  if (isLoading) {
    return <ActivityIndicator />;
  }

  return (
    <Stack screenOptions={{ headerShown: false }}>
      {!isAuthenticated ? (
        <Stack.Screen name="(auth)" options={{ headerShown: false }} />
      ) : (
        <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
      )}
    </Stack>
  );
}
```

### 2.3 Tab 布局 `app/(tabs)/_layout.tsx`

```typescript
/**
 * Tab 导航布局
 */
import { Tabs } from 'expo-router/tabs';
import { Map, Calendar } from '@/constants/icons';

export default function TabLayout() {
  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: '#6200EE',
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: '足迹',
          tabBarIcon: ({ color }) => <Map size={28} color={color} />,
        }}
      />
      <Tabs.Screen
        name="events"
        options={{
          title: '事件',
          tabBarIcon: ({ color }) => <Calendar size={28} color={color} />,
        }}
      />
    </Tabs>
  );
}
```

---

## 3. 完成检查清单

- [ ] `app/_layout.tsx` 已实现
- [ ] `app/(auth)/_layout.tsx` 已实现
- [ ] `app/(auth)/index.tsx` 欢迎页已实现
- [ ] `app/(tabs)/_layout.tsx` 已实现
- [ ] `app/(tabs)/index.tsx` 地图主页占位已创建
- [ ] 导航切换正常

---

**任务完成后，进入 [Task-19: 高德地图集成](./19-高德地图集成.md)**
